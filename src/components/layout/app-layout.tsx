'use client';

/**
 * Redirection file to maintain backward compatibility during restructuring.
 * The source of truth for the AppLayout is now in src/app/layout/app-layout.tsx
 */
export { AppLayout } from '@/app/layout/app-layout';
